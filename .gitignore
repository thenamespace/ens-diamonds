# OS
.DS_Store

# Node / pnpm (later phases)
node_modules/
.pnp.*
.turbo/
dist/
.next/
*.tsbuildinfo

# Foundry (packages/contracts)
# NOTE: lib/ is intentionally NOT ignored — Foundry deps (forge-std, etc.)
# live there as tracked git submodules.
out/
cache/
packages/contracts/broadcast/**/dry-run/

# Env / secrets
.env
.env.*

# Logs
*.log
npm-debug.log*
.vercel
.env*
!.env.example
!**/.env.example

# raw design assets (duplicated into apps/web/public)
/nsicons/
/diamonds-preview-image.png
