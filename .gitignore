# OS
.DS_Store

# Node / pnpm (later phases)
node_modules/
.pnp.*
.turbo/
dist/
.next/
*.tsbuildinfo

# Foundry (packages/contracts)
# NOTE: lib/ is intentionally NOT ignored — Foundry deps (forge-std, etc.)
# live there as tracked git submodules.
out/
cache/
broadcast/**/dry-run/

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
